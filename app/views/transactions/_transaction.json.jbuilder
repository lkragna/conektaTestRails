json.extract! transaction, :id, :tans_id, :name, :credit_card, :bin, :currency, :amount, :card_schema, :card_brand, :exp_date, :created_at, :updated_at
json.url transaction_url(transaction, format: :json)